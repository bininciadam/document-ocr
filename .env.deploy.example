# Copy to .env.deploy.production or .env.deploy.development and fill in.
# Both files are gitignored — never commit real values.

# Docker Hub — generate token at https://hub.docker.com/settings/security
DOCKERHUB_USERNAME=your-dockerhub-user
DOCKERHUB_TOKEN=

# Optional overrides (defaults are sensible)
# IMAGE_NAME=piyushgambhir/document-ocr
# DOCKERFILE_PATH=deploy/docker/Dockerfile
# BUILD_PLATFORMS=linux/amd64,linux/arm64

# --- GCP Cloud Run (uncomment once a project is chosen) ---
# GCP_CONFIG=your-gcloud-config         # gcloud config configuration name
# GCP_PROJECT=your-project-id
# GCP_REGION=asia-south1
# ALLOW_UNAUTHENTICATED=false           # opt in only behind your own auth layer
# DOCUMENT_OCR_API_TOKEN=               # optional Bearer token for /scan
# DOCUMENT_OCR_KYC_LANGS=en,devanagari  # optional KYC-only OCR model passes
